apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-dynamic
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: sc
  resources:
    requests:
      storage: 10Gi
